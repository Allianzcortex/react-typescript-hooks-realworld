import React from 'react';
import { render, screen } from '@testing-library/react';
import { Header } from '../Header';

test('render headers', () => {

// TODO set token
  expect(1+1).toBe(2)
});
